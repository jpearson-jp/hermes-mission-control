name: @NAME@
label: @LABEL@
kind: standalone
version: 1.1.0
description: >
  A Mission Control page in the Hermes web dashboard. This plugin owns no backend and no agent
  surface: its bundle fetches the hub plugin's read-only API (which reads every kanban board, every
  profile's cron store and the live run/event tables) and renders one view of the estate.
author: Hermes Smith
